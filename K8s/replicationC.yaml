apiVersion: v1
kind: ReplicationController
metadata:
   name: myapp
spec:
   replicas: 3
   selector:
       app: myapp
       templates:
        metadata:
           name: myapp
           labels:
              app: myapp
       spec:
           container:
              - name: myapp
                image: nginx
                ports:
                     - ContainerPort: 80               
